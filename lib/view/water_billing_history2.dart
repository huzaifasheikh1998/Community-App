import 'package:flutter/material.dart';

class WaterBillingHistory extends StatelessWidget {
  const WaterBillingHistory({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: [],
      ),
    );
  }
}
